{{ if eq .chezmoi.os "windows" }}
[cd]
    command = "pwsh"
    args = ["-NoLogo"]
{{- end }}

[merge]
    command = "nvim"
    args = [
        "-d",
        "{{ `{{ .Destination }}` }}",
        "{{ `{{ .Source }}` }}",
        "{{ `{{ .Target }}` }}",
    ]
